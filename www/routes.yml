/:
  controller: main
  action: home 

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/forget:
  controller: passwordrecovery
  action: pwdforget

/confirmation:
  controller: confirmation
  action: confirmation  

/reset-new-password:
  controller: confirmation
  action: confirmationPwd

/dashboard:
  controller: admin
  action: dashboard
  security:
    - admin
    - user

/payment:
  controller: payment
  action: payment


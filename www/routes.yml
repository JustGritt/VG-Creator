/:
  controller: main
  action: home 

/login:
  controller: user
  action: login

/login-fb:
  controller: user
  action: loginwithfb

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/forget:
  controller: passwordrecovery
  action: pwdforget

/confirmation:
  controller: confirmation
  action: confirmation  

/reset-new-password:
  controller: confirmation
  action: confirmationPwd

/dashboard:
  controller: admin
  action: dashboard
  security:
    - admin
    - user

/client_website:
  controller: admin
  action: client
/payment:
  controller: payment
  action: payment

